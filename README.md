# GBN
GBN和SR在错误处理机制，窗口大小，资源利用率和实现复杂度方面存在一些差异。

* **GBN协议**采用累计确认机制，接收方只能按序接收数据帧。当某个帧丢失时，发送方会重传从丢失帧开始的所有后续帧，即使这些帧可能已经正确到达接收方。这种机制实现简单，但在高丢包率环境下会造成大量不必要的重传，降低信道利用率。
* **SR协议**则采用选择性重传策略，接收方可以缓存乱序到达的帧，发送方只需重传真正丢失的帧。这种方式避免了不必要的重传，提高了信道利用率，但需要接收方维护更大的缓存空间，实现复杂度更高。

