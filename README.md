# RDT 2.0

RDT1.0 -> RDT2.0的转变是从理想信道到容错信道的转变（为了处理这种不可靠性，它采用了 停止等待(SW)机制）。

RDT1.0 假设信道完全可靠（无差错、无丢包、无延迟），发送方直接发送数据，接收方直接接收数据，无需任何确认机制。

RDT2.0 引入了信道可能会发生位错的假设，通过校验和和确认机制来实现差错检测和重传：

* 发送方计算数据包校验和，接收方验证校验和。
* 如果数据包正确（校验和匹配），接收方返回ACK；如果错误（校验和不匹配），则返回NAK。
* 发送方根据ACK/NAK决定发送下一分组或重传。
