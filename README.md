# TCP

TCP相对于GBN在发送方的**重传策略**，**接收方的缓存**，**ACK机制**等等方面有一些区别。

* 重传策略：
  * GBN是重传待已接收但未确认的整个窗口
  * 无拥塞控制的TCP支持快重传和选择性重传
* 接收方的缓存：
  * GBN不缓存乱序包
  * TCP缓存乱序包
* ACK机制：
  * GBN仅累计确认
  * TCP累计确认 + 立即乱序响应。
